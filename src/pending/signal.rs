

pub fn calculate_signal(
    ham: &HamiltonianTensors, 
    clusters, &Clusters, 
    config: &Config) -> Signal {

  // Loop over all cluster sizes.
    // Loop over all clusters of given size.
      // Calculate auxiliary signals.

  // do CCE
}
